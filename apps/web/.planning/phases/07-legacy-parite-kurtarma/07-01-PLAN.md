---
phase: 07-legacy-parite-kurtarma
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/src/components/kombin/StartScreen.tsx, apps/web/src/components/kombin/StartScreen.test.tsx, apps/web/src/lib/kombin/services/geminiService.ts, apps/web/src/app/api/ai/model/route.ts, apps/web/src/lib/ai/contracts.ts]
autonomous: true
requirements: [PARITY-01]
must_haves:
  truths:
    - "Model olusturma akisi eski calisan app davranisini yeniden vermeli"
    - "StartScreen runtime AI cagrisi browser-side provider yerine secure model endpoint'ine gitmeli"
    - "Proceed, deneysel ve manken degistir girisleri dogru moda gecmeli"
---

<objective>
Recover StartScreen and model-generation parity first so the new SaaS app regains a stable entry flow before deeper styling orchestration changes.

Purpose: PARITY-01 requires the first visible styling interaction to behave like the old working app while already obeying tenant-aware secure routing.

Output: StartScreen parity, secure model-generation path, passing component behavior tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-legacy-parite-kurtarma/07-legacy-parite-kurtarma-CONTEXT.md
@apps/web/src/components/kombin/StartScreen.tsx
@apps/web/src/components/kombin/StartScreen.test.tsx
@apps/web/src/components/kombin/KombinEditor.tsx
@apps/web/src/app/api/ai/model/route.ts
@apps/web/src/lib/kombin/services/geminiService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild StartScreen behavior parity on top of the secure model endpoint</name>
  <files>apps/web/src/components/kombin/StartScreen.tsx, apps/web/src/components/kombin/StartScreen.test.tsx, apps/web/src/lib/kombin/services/geminiService.ts, apps/web/src/app/api/ai/model/route.ts, apps/web/src/lib/ai/contracts.ts</files>
  <behavior>
    - Test 1: upload -> generating -> generated preview akisi gorunur
    - Test 2: standard styling CTA yalniz standart modu tetikler
    - Test 3: experimental CTA ayni model sonucuyla deneysel modu tetikler
    - Test 4: model swap girisi ayni generated image referansini kullanir
    - Test 5: runtime istegi sadece secure `/api/ai/model` yoluna gider
  </behavior>
  <action>Refactor `StartScreen.tsx` so model generation no longer depends on a browser-side Gemini provider implementation. Keep the old app's behavior contract: user uploads an image, sees a generating state, receives a generated model preview, and can continue into standard styling, experimental styling, or model swap using the same generated image. Replace any direct provider call path with a client wrapper that posts to `/api/ai/model` and carries the current workspace context expected by the route contract. Normalize loading, retry, and error states to match the old working flow instead of the current half-wired behavior.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/components/kombin/StartScreen.test.tsx</automated>
  </verify>
  <done>StartScreen once again behaves like the old app's working entry flow, but all runtime AI traffic goes through the secure model route.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/web test -- src/components/kombin/StartScreen.test.tsx`
- StartScreen network traffic uses `/api/ai/model`
</verification>
