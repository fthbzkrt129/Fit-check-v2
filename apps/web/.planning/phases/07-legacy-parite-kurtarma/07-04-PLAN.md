---
phase: 07-legacy-parite-kurtarma
plan: 04
type: execute
wave: 1
depends_on: [07-02-PLAN.md, 07-03-PLAN.md]
files_modified: [apps/web/src/components/kombin/KombinEditor.tsx, apps/web/src/components/kombin/WardrobeModal.tsx, apps/web/src/lib/kombin/experimentalBundle.ts, apps/web/src/lib/kombin/services/falService.ts, apps/web/src/lib/kombin/downloadImage.ts, apps/web/src/lib/kombin/wardrobePersistence.ts, apps/web/src/lib/kombin/sessionStorage.ts, apps/web/src/app/api/ai/experimental/route.ts]
autonomous: true
requirements: [PARITY-04, PARITY-05]
must_haves:
  truths:
    - "Deneysel one-shot bundled flow eski calisan davranisa geri donmeli"
    - "Parity'yi bozan persistence/history/download eksikleri bu fazda kapatilabilir"
    - "fal.ai runtime yolu yalniz secure experimental endpoint olmali"
---

<objective>
Recover the experimental bundled styling flow and finish the minimum persistence/history/download cleanup needed for the new app to feel parity-complete.

Purpose: PARITY-04 and PARITY-05 close the last obvious gaps between the old working styling experience and the new SaaS app.

Output: working experimental bundle flow, secure fal route usage, and parity-supporting persistence/download behavior.
</objective>

<context>
@.planning/phases/07-legacy-parite-kurtarma/07-legacy-parite-kurtarma-CONTEXT.md
@apps/web/src/components/kombin/KombinEditor.tsx
@apps/web/src/components/kombin/WardrobeModal.tsx
@apps/web/src/lib/kombin/experimentalBundle.ts
@apps/web/src/lib/kombin/downloadImage.ts
@apps/web/src/lib/kombin/wardrobePersistence.ts
@apps/web/src/lib/kombin/sessionStorage.ts
@apps/web/src/app/api/ai/experimental/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore experimental bundle parity and close minimum parity-supporting cleanup</name>
  <files>apps/web/src/components/kombin/KombinEditor.tsx, apps/web/src/components/kombin/WardrobeModal.tsx, apps/web/src/lib/kombin/experimentalBundle.ts, apps/web/src/lib/kombin/services/falService.ts, apps/web/src/lib/kombin/downloadImage.ts, apps/web/src/lib/kombin/wardrobePersistence.ts, apps/web/src/lib/kombin/sessionStorage.ts, apps/web/src/app/api/ai/experimental/route.ts</files>
  <behavior>
    - Test 1: staged garments tek bundled request ile sonuca doner
    - Test 2: duplicate submit ve retry davranisi parity ile calisir
    - Test 3: refresh sonrasi gerekli wardrobe/history/session state korunur
    - Test 4: download aktif image icin dogru output verir
    - Test 5: runtime traffic yalniz `/api/ai/experimental` endpoint'ine gider
  </behavior>
  <action>Bring back the old app's experimental bundled flow using the new secure fal route as the only runtime execution path. Ensure staged garments, final submit, retry handling, and duplicate-submit guards behave like the previously working implementation. Then fix only the persistence, history, and download issues that directly block parity-complete usage. Avoid unrelated cleanup and keep the scope tied to feature recovery.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/lib/kombin/experimentalBundle.test.ts src/lib/kombin/downloadImage.test.ts src/lib/kombin/wardrobePersistence.test.ts src/components/kombin/WardrobeModal.test.tsx</automated>
  </verify>
  <done>The experimental styling path works again in the SaaS app and the remaining persistence/download gaps no longer block parity-level usage.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/web test -- src/lib/kombin/experimentalBundle.test.ts src/lib/kombin/downloadImage.test.ts src/lib/kombin/wardrobePersistence.test.ts src/components/kombin/WardrobeModal.test.tsx`
- experimental traffic uses `/api/ai/experimental`
</verification>
