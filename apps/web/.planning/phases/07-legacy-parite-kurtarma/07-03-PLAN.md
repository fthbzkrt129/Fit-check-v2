---
phase: 07-legacy-parite-kurtarma
plan: 03
type: execute
wave: 1
depends_on: [07-02-PLAN.md]
files_modified: [apps/web/src/components/kombin/KombinEditor.tsx, apps/web/src/components/kombin/PosePanel.tsx, apps/web/src/components/kombin/ScenePanel.tsx, apps/web/src/components/kombin/SceneVariationList.tsx, apps/web/src/components/kombin/ModelSwapPanel.tsx, apps/web/src/lib/kombin/sceneVariations.ts, apps/web/src/lib/kombin/modelSwap.ts, apps/web/src/lib/kombin/services/geminiService.ts, apps/web/src/app/api/ai/pose/route.ts, apps/web/src/app/api/ai/scene/route.ts, apps/web/src/app/api/ai/model/route.ts]
autonomous: true
requirements: [PARITY-03]
must_haves:
  truths:
    - "Pose, scene ve model swap akislari styling parity'nin parcasi olarak geri donmeli"
    - "Bu varyasyon akislari history/canvas durumunu bozmamali"
    - "Runtime AI yollari ilgili secure endpoint'ler olmali"
---

<objective>
Recover the old app's pose, scene, and model-swap behaviors so advanced styling flows become trustworthy again in the SaaS app.

Purpose: PARITY-03 restores the higher-value editing flows that make the product feel complete rather than partially migrated.

Output: stable pose/scene/model-swap behavior with secure route wiring and testable state transitions.
</objective>

<context>
@.planning/phases/07-legacy-parite-kurtarma/07-legacy-parite-kurtarma-CONTEXT.md
@apps/web/src/components/kombin/KombinEditor.tsx
@apps/web/src/components/kombin/PosePanel.tsx
@apps/web/src/components/kombin/ScenePanel.tsx
@apps/web/src/components/kombin/SceneVariationList.tsx
@apps/web/src/components/kombin/ModelSwapPanel.tsx
@apps/web/src/lib/kombin/sceneVariations.ts
@apps/web/src/lib/kombin/modelSwap.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recover pose, scene, and model-swap parity on secure routes</name>
  <files>apps/web/src/components/kombin/KombinEditor.tsx, apps/web/src/components/kombin/PosePanel.tsx, apps/web/src/components/kombin/ScenePanel.tsx, apps/web/src/components/kombin/SceneVariationList.tsx, apps/web/src/components/kombin/ModelSwapPanel.tsx, apps/web/src/lib/kombin/sceneVariations.ts, apps/web/src/lib/kombin/modelSwap.ts, apps/web/src/lib/kombin/services/geminiService.ts, apps/web/src/app/api/ai/pose/route.ts, apps/web/src/app/api/ai/scene/route.ts, apps/web/src/app/api/ai/model/route.ts</files>
  <behavior>
    - Test 1: pose generation old app parity ile aktif image uzerinde calisir
    - Test 2: scene variation list eski hissi koruyarak secilebilir ve geri donulebilir
    - Test 3: model swap mevcut generated model ile uyumlu sekilde styling'e geri doner
    - Test 4: runtime traffic yalniz ilgili `/api/ai/pose`, `/api/ai/scene`, `/api/ai/model` endpoint'lerine gider
  </behavior>
  <action>Repair the advanced editing flows inside `KombinEditor` so they match the behavior that previously worked in the root app. Ensure pose generation chooses the correct current reference image, scene variation selection and rollback behave predictably, and model swap integrates cleanly with the current styling session. Remove any remaining runtime reliance on direct browser-side Gemini provider execution in these flows and route everything through the secure SaaS endpoints.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/components/kombin/ScenePanel.test.tsx src/components/kombin/SceneVariationList.test.tsx src/components/kombin/ModelSwapPanel.test.tsx src/lib/kombin/modelSwap.test.ts src/lib/kombin/sceneVariations.test.ts</automated>
  </verify>
  <done>Advanced styling flows once again behave like the old app while respecting the new server-side routing and tenant protection model.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/web test -- src/components/kombin/ScenePanel.test.tsx src/components/kombin/SceneVariationList.test.tsx src/components/kombin/ModelSwapPanel.test.tsx src/lib/kombin/modelSwap.test.ts src/lib/kombin/sceneVariations.test.ts`
- pose/scene/model-swap traffic uses secure AI routes only
</verification>
