---
phase: 07-legacy-parite-kurtarma
plan: 02
type: execute
wave: 1
depends_on: [07-01-PLAN.md]
files_modified: [apps/web/src/components/kombin/KombinEditor.tsx, apps/web/src/components/kombin/WardrobeModal.tsx, apps/web/src/components/kombin/CategoryStepPanel.tsx, apps/web/src/lib/kombin/outfitFlow.ts, apps/web/src/lib/kombin/types.ts, apps/web/src/lib/kombin/services/geminiService.ts, apps/web/src/app/api/ai/try-on/route.ts]
autonomous: true
requirements: [PARITY-02]
must_haves:
  truths:
    - "KombinEditor core try-on akisi eski calisan davranisa donmeli"
    - "Top, dress ve outerwear uzunluk secimleri parity ile calismali"
    - "Try-on runtime yolu yalniz `/api/ai/try-on` olmali"
---

<objective>
Recover the core styling orchestration inside `KombinEditor` so the new app once again behaves like the old working app during category-driven try-on and outfit history progression.

Purpose: PARITY-02 is the functional core of the product; without this, the new SaaS app has UI pieces but not a trustworthy styling flow.

Output: parity-aligned category orchestration, length handling, history updates, and secure try-on routing.
</objective>

<context>
@.planning/phases/07-legacy-parite-kurtarma/07-legacy-parite-kurtarma-CONTEXT.md
@apps/web/src/components/kombin/KombinEditor.tsx
@apps/web/src/components/kombin/WardrobeModal.tsx
@apps/web/src/components/kombin/CategoryStepPanel.tsx
@apps/web/src/lib/kombin/outfitFlow.ts
@apps/web/src/lib/kombin/types.ts
@apps/web/src/app/api/ai/try-on/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore category-driven try-on parity in KombinEditor</name>
  <files>apps/web/src/components/kombin/KombinEditor.tsx, apps/web/src/components/kombin/WardrobeModal.tsx, apps/web/src/components/kombin/CategoryStepPanel.tsx, apps/web/src/lib/kombin/outfitFlow.ts, apps/web/src/lib/kombin/types.ts, apps/web/src/lib/kombin/services/geminiService.ts, apps/web/src/app/api/ai/try-on/route.ts</files>
  <behavior>
    - Test 1: category secimi uygun next-step akisini korur
    - Test 2: top/dress/outerwear icin length selector parity ile calisir
    - Test 3: successful try-on current image ve history durumunu dogru gunceller
    - Test 4: standard mode browser-side provider yerine `/api/ai/try-on` uzerinden gider
  </behavior>
  <action>Refit `KombinEditor.tsx` around the old working orchestration contract instead of the current fragmented state handling. Keep a clear base image, current rendered image, outfit history, and category progression model. Reproduce the old app's length-dependent category behavior for top, dress, and outerwear. Remove any runtime dependency on direct Gemini provider calls in the standard try-on path and replace them with a secure wrapper targeting `/api/ai/try-on`. Update tests and helper modules only as needed to express the recovered behavior cleanly.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/components/kombin/CategoryStepPanel.test.tsx src/lib/kombin/outfitFlow.test.ts</automated>
  </verify>
  <done>The editor once again performs category-driven styling like the old app, while using the secure tenant-aware try-on route as its only runtime path.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/web test -- src/components/kombin/CategoryStepPanel.test.tsx src/lib/kombin/outfitFlow.test.ts`
- standard try-on traffic uses `/api/ai/try-on`
</verification>
