---
phase: 05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi
plan: 04
type: execute
wave: 3
depends_on: [05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-02, 05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-03]
files_modified: [apps/web/src/lib/ai/contracts.ts, apps/web/src/lib/ai/contracts.test.ts, apps/web/src/lib/ai/providers/geminiServer.ts, apps/web/src/lib/ai/providers/falServer.ts, apps/web/src/lib/ai/providers.test.ts, apps/web/src/lib/ai/requireWorkspaceAccess.ts, apps/web/src/app/api/ai/model/route.ts, apps/web/src/app/api/ai/try-on/route.ts, apps/web/src/app/api/ai/pose/route.ts, apps/web/src/app/api/ai/scene/route.ts, apps/web/src/app/api/ai/experimental/route.ts]
autonomous: true
requirements: [SAAS-04, SAAS-05]
user_setup:
  - service: gemini
    why: "server-side model generation"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio API keys"
  - service: fal.ai
    why: "experimental bundled outfit generation"
    env_vars:
      - name: FAL_KEY
        source: "fal.ai dashboard API keys"
must_haves:
  truths:
    - "Gemini and fal.ai keys are used only on the server"
    - "AI endpoints reject users outside the active workspace"
    - "Existing fit-check image-generation capabilities have a backend entrypoint in the new app"
  artifacts:
    - path: "apps/web/src/lib/ai/providers/geminiServer.ts"
      provides: "server-only Gemini adapter"
      exports: ["generateModelImage", "generateVirtualTryOnImage", "generatePoseVariation", "generateSceneVariation"]
    - path: "apps/web/src/lib/ai/providers/falServer.ts"
      provides: "server-only fal adapter"
      exports: ["generateExperimentalOutfitImage"]
    - path: "apps/web/src/app/api/ai/try-on/route.ts"
      provides: "tenant-aware backend try-on endpoint"
      exports: ["POST"]
  key_links:
    - from: "apps/web/src/app/api/ai/try-on/route.ts"
      to: "apps/web/src/lib/ai/requireWorkspaceAccess.ts"
      via: "workspace auth guard"
      pattern: "requireWorkspaceAccess"
    - from: "apps/web/src/lib/ai/providers/geminiServer.ts"
      to: "services/geminiService.ts"
      via: "ported prompt logic"
      pattern: "buildVirtualTryOnPrompt|buildScenePrompt"
    - from: "apps/web/src/lib/ai/providers/falServer.ts"
      to: "services/falService.ts"
      via: "ported experimental adapter behavior"
      pattern: "generateExperimentalOutfitImage|getExperimentalFalModel"
---

<objective>
Move the current AI provider surface behind authenticated, tenant-aware server endpoints so no provider key is exposed in the browser.

Purpose: per D-10 the whole reason for Phase 5 is to eliminate exposed API keys while preserving the existing fit-check generation flows inside a secure backend boundary.

Output: typed AI request contracts, server-only Gemini/fal adapters, workspace guard helper, and authenticated route handlers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-CONTEXT.md
@.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-DISCOVERY.md
@types.ts
@services/geminiService.ts
@services/falService.ts

<interfaces>
From `types.ts`:
```typescript
export type GarmentCategory = 'top' | 'outerwear' | 'dress' | 'bottom' | 'footwear' | 'accessory';
export type StylingMode = 'standard' | 'experimental';
export interface ExperimentalBundleInput {
  baseModelImage: string | File | Blob;
  imageInputs: Array<string | File | Blob>;
  garments: Array<{ id: string; name: string; category: GarmentCategory; imageIndex: number }>;
  finalSceneDescription?: string;
  prompt: string;
}
```

From `services/geminiService.ts`:
```typescript
export const buildScenePrompt = (...) => string;
export const buildVirtualTryOnPrompt = (...) => string;
export const generateModelImage = async (userImage: File): Promise<string> => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define server-side AI contracts and provider adapters</name>
  <files>apps/web/src/lib/ai/contracts.ts, apps/web/src/lib/ai/contracts.test.ts, apps/web/src/lib/ai/providers/geminiServer.ts, apps/web/src/lib/ai/providers/falServer.ts, apps/web/src/lib/ai/providers.test.ts</files>
  <behavior>
    - Test 1: request schemas reject invalid categories/payload shapes before any provider call
    - Test 2: Gemini adapter reads keys from server env only and preserves existing prompt-builder behavior
    - Test 3: fal adapter keeps the bounded-retry + normalized-error pattern established in Phase 4
  </behavior>
  <action>Create typed request/response contracts for model generation, try-on, pose, scene, and experimental bundle actions. Port the current prompt composition and adapter behavior from `services/geminiService.ts` and `services/falService.ts` into server-only modules under `apps/web/src/lib/ai/providers/`. Keep user-facing error normalization and bounded retry behavior from Phase 4, but remove all client-side `process.env` usage so provider keys never ship to the browser. Do not wire routes in this task.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/lib/ai/contracts.test.ts src/lib/ai/providers.test.ts</automated>
  </verify>
  <done>The new app has server-only AI adapters with typed validation and no browser secret dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose tenant-aware AI route handlers</name>
  <files>apps/web/src/lib/ai/requireWorkspaceAccess.ts, apps/web/src/app/api/ai/model/route.ts, apps/web/src/app/api/ai/try-on/route.ts, apps/web/src/app/api/ai/pose/route.ts, apps/web/src/app/api/ai/scene/route.ts, apps/web/src/app/api/ai/experimental/route.ts</files>
  <behavior>
    - Test 1: unauthenticated or wrong-workspace requests get rejected before provider code runs
    - Test 2: authenticated workspace members can call the standard Gemini-backed endpoints
    - Test 3: authenticated workspace members can call the experimental fal endpoint with normalized JSON responses
  </behavior>
  <action>Implement a reusable `requireWorkspaceAccess()` guard that resolves the Supabase user and confirms membership against the workspace implied by the subdomain/middleware context. Then add route handlers for `model`, `try-on`, `pose`, `scene`, and `experimental` actions. Each route must validate input against Task 1 contracts, call the correct server-only provider adapter, and return typed JSON without leaking raw provider exceptions or any secret material. Keep the API surface compatible with the current fit-check feature set, but do not rebuild the full client workflow UI in this plan.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/lib/ai/contracts.test.ts src/lib/ai/providers.test.ts && npm --prefix apps/web run build</automated>
  </verify>
  <done>All AI generation entrypoints are backend-only, membership-guarded, and ready for a future UI migration onto the new SaaS shell.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/web test -- src/lib/ai/contracts.test.ts src/lib/ai/providers.test.ts`
- `npm --prefix apps/web run build`
- Confirm no `GEMINI_API_KEY`, `API_KEY`, or `FAL_KEY` references remain in browser-facing app code under `apps/web/src/app`
</verification>

<success_criteria>
- Provider keys exist only in server env access paths
- Workspace membership is checked before AI work runs
- Standard and experimental fit-check generation both have secure backend endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-04-SUMMARY.md`
</output>
