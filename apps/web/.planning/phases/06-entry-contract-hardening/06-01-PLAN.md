---
phase: 06-entry-contract-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/src/lib/tenant/entryContract.ts, apps/web/src/lib/tenant/entryContract.test.ts, apps/web/src/lib/tenant/resolveTenantHost.ts, apps/web/src/lib/tenant/buildWorkspaceUrl.ts, apps/web/middleware.ts]
autonomous: true
requirements: [FLOW-02]
must_haves:
  truths:
    - "Ziyaretci root domaine geldiginde marketing surface tenant rewrite carpismina girmeden acilir"
    - "Tenant host uzerinden gelen anonim istek root login yuzeyine workspace niyetini kaybetmeden yonlenir"
    - "Authenticated tenant host istegi yalnizca tenant shell'e rewrite edilir"
  artifacts:
    - path: "apps/web/src/lib/tenant/entryContract.ts"
      provides: "root-vs-tenant entry contract helpers"
      exports: ["getEntryRedirectIntent", "getTenantRewritePath", "isBypassedPath"]
    - path: "apps/web/src/lib/tenant/entryContract.test.ts"
      provides: "entry contract coverage for root, tenant, localhost, preview"
    - path: "apps/web/middleware.ts"
      provides: "host-aware auth redirect and tenant rewrite gate"
      exports: ["middleware", "config"]
  key_links:
    - from: "apps/web/middleware.ts"
      to: "apps/web/src/lib/tenant/entryContract.ts"
      via: "redirect/rewrite intent resolution"
      pattern: "getEntryRedirectIntent|getTenantRewritePath|isBypassedPath"
    - from: "apps/web/src/lib/tenant/entryContract.ts"
      to: "apps/web/src/lib/tenant/resolveTenantHost.ts"
      via: "hostname classification"
      pattern: "resolveTenantHost"
    - from: "apps/web/src/lib/tenant/entryContract.ts"
      to: "apps/web/src/lib/tenant/buildWorkspaceUrl.ts"
      via: "canonical workspace URL building"
      pattern: "buildWorkspaceUrl"
---

<objective>
Harden the shared entry-routing contract so the root marketing surface and tenant subdomain gate behave predictably before auth handoff logic is touched.

Purpose: FLOW-02 requires root marketing traffic, tenant rewrites, and login redirects to share one deterministic contract instead of scattered logic.

Output: shared entry contract helpers/tests plus a narrower, safer middleware implementation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-entry-contract-hardening/06-RESEARCH.md
@.planning/phases/06-entry-contract-hardening/06-VALIDATION.md
@apps/web/middleware.ts
@apps/web/src/lib/tenant/resolveTenantHost.ts
@apps/web/src/lib/tenant/buildWorkspaceUrl.ts
@apps/web/src/lib/supabase/middleware.ts

<interfaces>
From `apps/web/src/lib/tenant/resolveTenantHost.ts`:
```typescript
export type ResolvedTenantHost =
  | { kind: "root"; isTenant: false; workspaceSlug: null; hostname: string; rootHost: string }
  | { kind: "tenant"; isTenant: true; workspaceSlug: string; hostname: string; rootHost: string };

export const resolveTenantHost = (host: string, rootDomain: string): ResolvedTenantHost => { ... };
```

From `apps/web/src/lib/tenant/buildWorkspaceUrl.ts`:
```typescript
export const buildWorkspaceUrl = (
  workspaceSlug: string,
  rootDomain: string,
  options?: { protocol?: "http" | "https"; pathname?: string; search?: string }
) => string;
```

From `apps/web/src/lib/supabase/middleware.ts`:
```typescript
export const updateSession = async (request: NextRequest) => {
  return { response, user, supabase };
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the root-versus-tenant entry contract into a pure helper</name>
  <files>apps/web/src/lib/tenant/entryContract.ts, apps/web/src/lib/tenant/entryContract.test.ts, apps/web/src/lib/tenant/resolveTenantHost.ts, apps/web/src/lib/tenant/buildWorkspaceUrl.ts</files>
  <read_first>apps/web/src/lib/tenant/resolveTenantHost.ts, apps/web/src/lib/tenant/buildWorkspaceUrl.ts, apps/web/middleware.ts, .planning/phases/06-entry-contract-hardening/06-RESEARCH.md</read_first>
  <behavior>
    - Test 1: root hosts (`fitcheck.app`, `www.fitcheck.app`, `localhost:3000`) stay on the shared surface and do not produce tenant rewrite intents
    - Test 2: tenant hosts (`styling.fitcheck.app`, `styling.localhost:3000`, `styling---preview.vercel.app`) resolve to workspace `styling`
    - Test 3: unauthenticated tenant access generates root login redirect intent with `?next=styling`
    - Test 4: authenticated tenant access generates internal rewrite path `/_tenant/styling` while preserving the original pathname suffix
  </behavior>
  <action>Create `apps/web/src/lib/tenant/entryContract.ts` as a pure helper module that centralizes Phase 06 entry rules. Export `isBypassedPath(pathname: string)`, `getEntryRedirectIntent(args)`, and `getTenantRewritePath(workspaceSlug: string, pathname: string)`. `isBypassedPath` must return true for `/api`, `/_next/static`, `/_next/image`, `/favicon.ico`, `/sitemap.xml`, and `/robots.txt`. `getEntryRedirectIntent` must classify root vs tenant hosts using `resolveTenantHost`, return `{ kind: "root-pass" }` for non-tenant hosts, return `{ kind: "login-redirect", loginUrl: string, workspaceSlug: string }` for unauthenticated tenant access using the canonical root login URL and `next` query param, and return `{ kind: "tenant-rewrite", workspaceSlug: string, pathname: string }` for authenticated tenant access. Extend or adjust `resolveTenantHost` / `buildWorkspaceUrl` only if required to support these exact cases; do not introduce organization slugs, multi-workspace switching, or query-param-based tenant detection.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/lib/tenant/entryContract.test.ts src/lib/tenant/tenantRouting.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/tenant/entryContract.ts` contains `export const isBypassedPath`
    - `apps/web/src/lib/tenant/entryContract.ts` contains `export const getEntryRedirectIntent`
    - `apps/web/src/lib/tenant/entryContract.ts` contains `export const getTenantRewritePath`
    - `apps/web/src/lib/tenant/entryContract.test.ts` contains a test asserting `/login?next=styling`
    - `apps/web/src/lib/tenant/entryContract.test.ts` contains a test asserting `/_tenant/styling`
    - `npm --prefix apps/web test -- src/lib/tenant/entryContract.test.ts src/lib/tenant/tenantRouting.test.ts` exits 0
  </acceptance_criteria>
  <done>Shared entry-routing rules are expressed in one pure helper with passing tests for root, tenant, localhost, and preview host cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refit middleware to the shared contract and narrow the matcher</name>
  <files>apps/web/middleware.ts, apps/web/src/lib/tenant/entryContract.ts, apps/web/src/lib/tenant/entryContract.test.ts</files>
  <read_first>apps/web/middleware.ts, apps/web/src/lib/tenant/entryContract.ts, apps/web/src/lib/supabase/middleware.ts, .planning/phases/06-entry-contract-hardening/06-RESEARCH.md</read_first>
  <behavior>
    - Test 1: root marketing and auth routes pass through without tenant rewrite
    - Test 2: unauthenticated tenant traffic redirects to root `/login?next={workspaceSlug}`
    - Test 3: authenticated tenant traffic rewrites to `/_tenant/{workspaceSlug}{pathname}`
    - Test 4: matcher excludes `api`, static/image assets, and metadata files from middleware processing
  </behavior>
  <action>Update `apps/web/middleware.ts` to consume `isBypassedPath`, `getEntryRedirectIntent`, and `getTenantRewritePath` instead of open-coded redirect/rewrite logic. Keep the `updateSession(request)` + `auth.getUser()` Supabase SSR pattern intact. For non-tenant hosts, allow `/`, `/login`, `/auth/callback`, and `/auth/finish-signup` to continue without tenant rewrite. For tenant hosts, redirect anonymous users to the root-domain login URL produced by the helper and rewrite authenticated users to the tenant shell path produced by the helper. Change `config.matcher` from the current broad catch-all to a negative-lookahead pattern that excludes `api`, `_next/static`, `_next/image`, `favicon.ico`, `sitemap.xml`, and `robots.txt`. Do not add homepage styling, new auth providers, or extra middleware branches unrelated to root-vs-tenant entry safety.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/lib/tenant/entryContract.test.ts src/lib/tenant/tenantRouting.test.ts && npm --prefix apps/web run build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/middleware.ts` imports from `@/lib/tenant/entryContract`
    - `apps/web/middleware.ts` still calls `updateSession(request)`
    - `apps/web/middleware.ts` contains a matcher excluding `api|_next/static|_next/image|favicon.ico|sitemap.xml|robots.txt`
    - `apps/web/middleware.ts` no longer hardcodes `loginUrl.searchParams.set("next", tenantHost.workspaceSlug)` directly
    - `npm --prefix apps/web run build` exits 0
  </acceptance_criteria>
  <done>Middleware behavior is driven by the shared entry contract, tenant-only rewrites are explicit, and non-app/static routes are excluded from the middleware surface.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/web test -- src/lib/tenant/entryContract.test.ts src/lib/tenant/tenantRouting.test.ts`
- `npm --prefix apps/web run build`
- `apps/web/middleware.ts` imports and uses `@/lib/tenant/entryContract`
</verification>

<success_criteria>
- Root marketing traffic remains on the shared surface
- Tenant host redirects and rewrites are derived from one pure contract
- Middleware no longer applies to excluded `api`/static/metadata paths
</success_criteria>

<output>
After completion, create `.planning/phases/06-entry-contract-hardening/06-01-SUMMARY.md`
</output>
