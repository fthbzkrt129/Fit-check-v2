---
phase: 06-entry-contract-hardening
plan: 02
type: execute
wave: 2
depends_on: [06-entry-contract-hardening-01]
files_modified: [apps/web/src/app/login/page.tsx, apps/web/src/app/auth/callback/route.ts, apps/web/src/app/auth/finish-signup/route.ts, apps/web/src/lib/tenant/entryContract.ts, apps/web/src/lib/tenant/entryContract.test.ts, apps/web/src/lib/auth/messages.ts]
autonomous: true
requirements: [FLOW-02, FLOW-03]
must_haves:
  truths:
    - "Kullanici tenant hosttan login'e dusunce hedef workspace niyeti korunur"
    - "Auth callback sonrasi mevcut `next` veya kullanici metadata'sindaki workspace ile dogru domaine gider"
    - "Finish-signup fallback'i de ayni canonical workspace redirect sozlesmesini kullanir"
  artifacts:
    - path: "apps/web/src/app/login/page.tsx"
      provides: "next-aware auth submit surface"
    - path: "apps/web/src/app/auth/callback/route.ts"
      provides: "session exchange and final redirect resolution"
      exports: ["GET"]
    - path: "apps/web/src/app/auth/finish-signup/route.ts"
      provides: "bootstrap fallback and final redirect resolution"
      exports: ["GET"]
  key_links:
    - from: "apps/web/src/app/login/page.tsx"
      to: "apps/web/src/lib/tenant/entryContract.ts"
      via: "preserved next/workspace intent"
      pattern: "next|workspace|entryContract"
    - from: "apps/web/src/app/auth/callback/route.ts"
      to: "apps/web/src/lib/tenant/entryContract.ts"
      via: "post-auth redirect resolution"
      pattern: "entryContract|buildWorkspaceUrl"
    - from: "apps/web/src/app/auth/finish-signup/route.ts"
      to: "apps/web/src/lib/tenant/bootstrapWorkspace.ts"
      via: "workspace bootstrap before final redirect"
      pattern: "bootstrapWorkspaceForUser"
---

<objective>
Unify login, callback, and finish-signup around the same post-auth handoff contract so workspace intent survives every supported auth path.

Purpose: FLOW-03 depends on a single canonical handoff path from tenant redirect -> login -> callback/bootstrap -> workspace domain.

Output: hardened auth handoff logic with focused automated tests for preserved `next` and final workspace redirects.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-entry-contract-hardening/06-RESEARCH.md
@.planning/phases/06-entry-contract-hardening/06-VALIDATION.md
@.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-02-SUMMARY.md
@.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-03-SUMMARY.md
@apps/web/src/app/login/page.tsx
@apps/web/src/app/auth/callback/route.ts
@apps/web/src/app/auth/finish-signup/route.ts
@apps/web/src/lib/tenant/bootstrapWorkspace.ts
@apps/web/src/lib/tenant/entryContract.ts

<interfaces>
From `apps/web/src/app/auth/callback/route.ts`:
```typescript
export async function GET(request: NextRequest) {
  const code = request.nextUrl.searchParams.get("code");
  const nextWorkspace = request.nextUrl.searchParams.get("next");
}
```

From `apps/web/src/app/auth/finish-signup/route.ts`:
```typescript
export async function GET(request: NextRequest) {
  const workspace = await bootstrapWorkspaceForUser({ userId, email, fullName });
  return NextResponse.redirect(buildWorkspaceUrl(workspace.workspaceSlug, rootDomain));
}
```

Expected from Plan 01:
```typescript
export const getEntryRedirectIntent: (...) =>
  | { kind: "root-pass" }
  | { kind: "login-redirect"; loginUrl: string; workspaceSlug: string }
  | { kind: "tenant-rewrite"; workspaceSlug: string; pathname: string };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize post-auth redirect resolution for login and callback</name>
  <files>apps/web/src/app/login/page.tsx, apps/web/src/app/auth/callback/route.ts, apps/web/src/lib/tenant/entryContract.ts, apps/web/src/lib/tenant/entryContract.test.ts, apps/web/src/lib/auth/messages.ts</files>
  <read_first>apps/web/src/app/login/page.tsx, apps/web/src/app/auth/callback/route.ts, apps/web/src/lib/tenant/entryContract.ts, apps/web/src/lib/auth/messages.ts, .planning/phases/06-entry-contract-hardening/06-RESEARCH.md</read_first>
  <behavior>
    - Test 1: login submit paths keep the `next` workspace value for magic-link and password flows
    - Test 2: callback resolves final workspace by prioritizing `next` query, then `user_metadata.workspace_slug`, then fallback route
    - Test 3: callback rejects missing/unsafe workspace targets and sends the user to a safe root-domain login or finish-signup path instead of arbitrary redirect targets
  </behavior>
  <action>Extend `apps/web/src/lib/tenant/entryContract.ts` with pure helpers for post-auth redirect resolution, such as `getRequestedWorkspace(requestUrlOrSearchParams)` and `resolvePostAuthWorkspace({ requestedWorkspace, userWorkspaceSlug })`. Refactor `apps/web/src/app/login/page.tsx` so magic-link callback URLs and password-login/password-signup finish-signup URLs are built through these helpers instead of open-coded `URLSearchParams` logic. Refactor `apps/web/src/app/auth/callback/route.ts` so it exchanges the auth code, derives the final workspace slug from `next` first and `user.user_metadata.workspace_slug` second, and redirects to `/auth/finish-signup` only when no workspace slug can be resolved. Keep `getAuthStatusMessage()` as the user-facing error mapping source; only extend it if the refactor introduces a new auth error string that needs translation.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/lib/tenant/entryContract.test.ts src/lib/auth/messages.test.ts && npm --prefix apps/web run build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/tenant/entryContract.ts` contains a helper that reads or resolves the requested workspace
    - `apps/web/src/app/login/page.tsx` no longer manually constructs both callback and finish-signup `next` propagation in separate inline blocks
    - `apps/web/src/app/auth/callback/route.ts` contains a `next`-first then `user_metadata.workspace_slug` fallback order
    - `apps/web/src/app/auth/callback/route.ts` still calls `exchangeCodeForSession(code)`
    - `npm --prefix apps/web test -- src/lib/tenant/entryContract.test.ts src/lib/auth/messages.test.ts` exits 0
  </acceptance_criteria>
  <done>Login and callback flows share the same workspace-intent contract, preserving `next` across supported auth modes without open-coded duplication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make finish-signup use the same canonical workspace redirect contract</name>
  <files>apps/web/src/app/auth/finish-signup/route.ts, apps/web/src/lib/tenant/entryContract.ts, apps/web/src/lib/tenant/entryContract.test.ts, apps/web/src/lib/tenant/bootstrapWorkspace.ts</files>
  <read_first>apps/web/src/app/auth/finish-signup/route.ts, apps/web/src/lib/tenant/bootstrapWorkspace.ts, apps/web/src/lib/tenant/entryContract.ts, .planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-03-SUMMARY.md</read_first>
  <behavior>
    - Test 1: finish-signup redirects an authenticated user to the canonical workspace URL after bootstrap
    - Test 2: if a requested workspace already exists and matches the authenticated user's workspace, the same canonical redirect is used
    - Test 3: if there is no authenticated user email, finish-signup safely returns to root `/login`
  </behavior>
  <action>Refactor `apps/web/src/app/auth/finish-signup/route.ts` to use the same entry-contract helper(s) from Task 1 for final redirect building instead of directly assembling the destination inline. Preserve the existing `bootstrapWorkspaceForUser()` behavior and database contract from Phase 05; do not change schema, membership logic, or service-role usage. If needed, thread an optional requested workspace slug through the redirect helper so finish-signup can prefer the bootstrap result while still keeping canonical workspace-domain formatting identical to callback redirects. Add or extend tests in `entryContract.test.ts` (or a nearby focused auth-flow test file if cleaner) so finish-signup redirect outcomes are covered alongside callback outcomes.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/lib/tenant/entryContract.test.ts src/lib/tenant/bootstrapWorkspace.test.ts && npm --prefix apps/web run build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/auth/finish-signup/route.ts` imports from `@/lib/tenant/entryContract`
    - `apps/web/src/app/auth/finish-signup/route.ts` still imports and calls `bootstrapWorkspaceForUser`
    - `apps/web/src/app/auth/finish-signup/route.ts` no longer directly calls `buildWorkspaceUrl(` without going through the shared helper
    - test coverage includes a finish-signup redirect assertion
    - `npm --prefix apps/web run build` exits 0
  </acceptance_criteria>
  <done>Finish-signup and callback now converge on the same canonical workspace redirect behavior, closing the auth handoff gap for FLOW-03.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/web test -- src/lib/tenant/entryContract.test.ts src/lib/auth/messages.test.ts src/lib/tenant/bootstrapWorkspace.test.ts`
- `npm --prefix apps/web run build`
- Confirm `login`, `auth/callback`, and `auth/finish-signup` all import the shared entry contract helpers
</verification>

<success_criteria>
- `next` / workspace intent survives tenant -> login -> callback or finish-signup flow
- Callback and finish-signup use one canonical workspace redirect contract
- Root login fallback remains safe when no authenticated user or workspace target exists
</success_criteria>

<output>
After completion, create `.planning/phases/06-entry-contract-hardening/06-02-SUMMARY.md`
</output>
