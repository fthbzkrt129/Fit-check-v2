---
phase: 05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi
plan: 02
type: execute
wave: 2
depends_on: [05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-01]
files_modified: [apps/web/src/lib/tenant/resolveTenantHost.ts, apps/web/src/lib/tenant/buildWorkspaceUrl.ts, apps/web/src/lib/tenant/tenantRouting.test.ts, apps/web/src/lib/supabase/browser.ts, apps/web/src/lib/supabase/server.ts, apps/web/src/lib/supabase/middleware.ts, apps/web/middleware.ts, apps/web/src/app/login/page.tsx, apps/web/src/app/auth/callback/route.ts, apps/web/src/app/_tenant/[workspaceSlug]/layout.tsx, apps/web/src/app/_tenant/[workspaceSlug]/page.tsx]
autonomous: true
requirements: [SAAS-02]
user_setup:
  - service: supabase
    why: "Supabase Auth redirect + cookie flow"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase project settings -> API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase project settings -> API"
    dashboard_config:
      - task: "Set Site URL to the root domain and add login/callback redirect URLs for local + production"
        location: "Supabase Dashboard -> Authentication -> URL Configuration"
  - service: vercel
    why: "Workspace subdomain routing"
    dashboard_config:
      - task: "Attach root domain and wildcard subdomain (*.your-domain.com)"
        location: "Vercel Project -> Domains"
must_haves:
  truths:
    - "Root domain traffic stays on the shared landing/login surface"
    - "Workspace subdomain traffic resolves to a tenant-aware app shell"
    - "Authenticated users are redirected from auth callback to their workspace domain"
  artifacts:
    - path: "apps/web/middleware.ts"
      provides: "host-aware routing and auth guard"
    - path: "apps/web/src/lib/tenant/resolveTenantHost.ts"
      provides: "workspace/root-domain host parsing"
      exports: ["resolveTenantHost"]
    - path: "apps/web/src/app/auth/callback/route.ts"
      provides: "Supabase auth code exchange and post-login redirect"
      exports: ["GET"]
  key_links:
    - from: "apps/web/middleware.ts"
      to: "apps/web/src/lib/tenant/resolveTenantHost.ts"
      via: "host parsing"
      pattern: "resolveTenantHost"
    - from: "apps/web/middleware.ts"
      to: "apps/web/src/lib/supabase/middleware.ts"
      via: "SSR auth client"
      pattern: "createServerClient|getUser"
    - from: "apps/web/src/app/auth/callback/route.ts"
      to: "apps/web/src/lib/tenant/buildWorkspaceUrl.ts"
      via: "final redirect after code exchange"
      pattern: "buildWorkspaceUrl"
---

<objective>
Implement tenant-aware auth and subdomain routing so the new app can distinguish the shared landing domain from workspace domains and redirect authenticated users into their single workspace.

Purpose: per D-03, D-06, D-07, and D-08 the app must resolve tenant context from the hostname and keep auth state server-side.

Output: tenant host parser/tests, Supabase SSR clients, middleware, login/callback flow, and tenant app shell.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-CONTEXT.md
@.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-DISCOVERY.md
@apps/web/src/lib/env.ts

<interfaces>
Expected from Plan 01:
```typescript
export function getPublicEnv(): {
  supabaseUrl: string;
  supabaseAnonKey: string;
  rootDomain: string;
};
export function getServerEnv(): {
  supabaseServiceRoleKey: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define tenant host parsing and workspace URL contracts</name>
  <files>apps/web/src/lib/tenant/resolveTenantHost.ts, apps/web/src/lib/tenant/buildWorkspaceUrl.ts, apps/web/src/lib/tenant/tenantRouting.test.ts</files>
  <behavior>
    - Test 1: `workspace.example.com` resolves to workspace `workspace` and marks the request as tenant-scoped
    - Test 2: root hosts (`example.com`, `www.example.com`, localhost root) remain non-tenant landing/auth hosts
    - Test 3: local development and Vercel preview hosts still normalize to `<workspace>.<root-domain>` redirect targets
  </behavior>
  <action>Implement host parsing helpers per D-06 and the discovery decision to use `<workspace>.<root-domain>`. `resolveTenantHost()` must recognize production, localhost, and Vercel preview hostnames without trusting client-provided tenant ids. `buildWorkspaceUrl()` must emit the canonical post-auth redirect target for a single workspace per D-08. Reserve non-tenant hostnames like `www`, `app`, and `admin` for the shared surface; do not introduce organization slugs in the URL and do not add multi-workspace switching.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/lib/tenant/tenantRouting.test.ts</automated>
  </verify>
  <done>Hostname parsing is deterministic, tested, and ready for middleware/auth redirect wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Supabase SSR auth, middleware, and the tenant app shell</name>
  <files>apps/web/src/lib/supabase/browser.ts, apps/web/src/lib/supabase/server.ts, apps/web/src/lib/supabase/middleware.ts, apps/web/middleware.ts, apps/web/src/app/login/page.tsx, apps/web/src/app/auth/callback/route.ts, apps/web/src/app/_tenant/[workspaceSlug]/layout.tsx, apps/web/src/app/_tenant/[workspaceSlug]/page.tsx</files>
  <behavior>
    - Test 1: middleware uses `getUser()` through `@supabase/ssr` and redirects unauthenticated tenant requests to `/login`
    - Test 2: auth callback exchanges the code for a session and redirects to the canonical workspace URL
    - Test 3: authenticated tenant requests land inside the internal tenant app shell without exposing the workspace slug on the public root domain
  </behavior>
  <action>Create `createBrowserClient` and `createServerClient` helpers with cookie wiring using `@supabase/ssr`, and a dedicated middleware helper that refreshes auth state server-side. In `apps/web/middleware.ts`, combine the Supabase auth refresh with tenant host parsing from Task 1. Root-domain requests should continue to the landing/login experience per D-07; tenant subdomain requests should rewrite into `/_tenant/[workspaceSlug]` and require an authenticated user. `src/app/auth/callback/route.ts` must exchange the auth code for a session, resolve the user's single workspace target per D-08, and redirect there. Add a minimal login page and tenant layout/page shell; do not implement full onboarding or database bootstrap here.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/lib/tenant/tenantRouting.test.ts && npm --prefix apps/web run build</automated>
  </verify>
  <done>The app now separates landing vs tenant domains, refreshes Supabase auth on the server, and has a working post-login redirect surface.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/web test -- src/lib/tenant/tenantRouting.test.ts`
- `npm --prefix apps/web run build`
- Verify `middleware.ts` calls `getUser()` instead of trusting `getSession()` only
</verification>

<success_criteria>
- Tenant context is derived from hostname, not client-supplied identifiers
- Root and tenant domains behave differently but predictably
- Supabase Auth session exchange happens server-side and leads to workspace redirect
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-02-SUMMARY.md`
</output>
