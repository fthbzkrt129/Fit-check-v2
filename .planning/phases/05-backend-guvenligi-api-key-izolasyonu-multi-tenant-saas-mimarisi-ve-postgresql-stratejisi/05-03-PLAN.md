---
phase: 05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi
plan: 03
type: execute
wave: 2
depends_on: [05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-01]
files_modified: [apps/web/supabase/config.toml, apps/web/supabase/migrations/20260403_000001_tenant_foundation.sql, apps/web/supabase/tests/tenant_foundation.pgtap.sql, apps/web/src/lib/supabase/admin.ts, apps/web/src/lib/tenant/bootstrapWorkspace.ts, apps/web/src/lib/tenant/bootstrapWorkspace.test.ts, apps/web/src/app/auth/finish-signup/route.ts]
autonomous: true
requirements: [SAAS-03]
user_setup:
  - service: supabase
    why: "database migrations, RLS, and bootstrap admin access"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase project settings -> API"
    dashboard_config:
      - task: "Create the project and enable email/password auth before running bootstrap flow"
        location: "Supabase Dashboard -> Project + Authentication"
must_haves:
  truths:
    - "Every authenticated user sees only their own workspace-bound tenant records"
    - "First-time signup creates exactly one organization, one workspace, and one owner membership"
    - "Cross-workspace reads are blocked by RLS rather than only app code"
  artifacts:
    - path: "apps/web/supabase/migrations/20260403_000001_tenant_foundation.sql"
      provides: "tenant schema and RLS foundation"
      contains: "create table public.workspaces"
    - path: "apps/web/supabase/tests/tenant_foundation.pgtap.sql"
      provides: "db-level isolation tests"
    - path: "apps/web/src/lib/tenant/bootstrapWorkspace.ts"
      provides: "automatic org/workspace bootstrap helper"
      exports: ["bootstrapWorkspaceForUser"]
  key_links:
    - from: "apps/web/src/lib/tenant/bootstrapWorkspace.ts"
      to: "apps/web/src/lib/supabase/admin.ts"
      via: "service-role server client"
      pattern: "createClient|service_role"
    - from: "apps/web/src/app/auth/finish-signup/route.ts"
      to: "apps/web/src/lib/tenant/bootstrapWorkspace.ts"
      via: "post-auth provisioning"
      pattern: "bootstrapWorkspaceForUser"
    - from: "apps/web/supabase/tests/tenant_foundation.pgtap.sql"
      to: "apps/web/supabase/migrations/20260403_000001_tenant_foundation.sql"
      via: "policy verification"
      pattern: "policy|enable row level security"
---

<objective>
Create the minimal Supabase tenant schema, RLS policies, and first-login bootstrap path for the new SaaS foundation.

Purpose: per D-02, D-04, D-05, D-08, and D-10 the product needs database-backed tenant isolation before any secure backend features can rely on workspace context.

Output: Supabase config, migration, db tests, admin client, and automatic organization/workspace bootstrap route.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-CONTEXT.md
@.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-DISCOVERY.md
@apps/web/src/lib/env.ts

<interfaces>
Chosen tenant model:
```typescript
type TenantHierarchy = {
  organization: { id: string; slug: string; name: string };
  workspace: { id: string; slug: string; organizationId: string };
  membership: { userId: string; workspaceId: string; role: 'owner' | 'member' };
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the tenant foundation schema and RLS policies</name>
  <files>apps/web/supabase/config.toml, apps/web/supabase/migrations/20260403_000001_tenant_foundation.sql, apps/web/supabase/tests/tenant_foundation.pgtap.sql</files>
  <behavior>
    - Test 1: an authenticated user can read their own profile and workspace membership rows
    - Test 2: a user cannot read another workspace's rows even if they guess ids
    - Test 3: workspace slug uniqueness and single-membership constraints prevent duplicate tenant bootstrap for one user
  </behavior>
  <action>Set up Supabase local config and create a minimal migration for D-04 and D-05 with only the core foundation tables: `profiles`, `organizations`, `workspaces`, and `workspace_memberships`. Use UUID primary keys, slugs, timestamps, and only the essential columns needed for hostname resolution and membership checks; do not add billing, invites, AI jobs, or broad product schema from the deferred items. Enable RLS on every public table and add the minimum policies/functions required to scope reads/writes by `auth.uid()` and workspace membership. Add pgTAP coverage in `tenant_foundation.pgtap.sql` so cross-workspace isolation is proven at the database layer.</action>
  <verify>
    <automated>npm --prefix apps/web run test:db</automated>
  </verify>
  <done>The repo contains a runnable Supabase migration plus db tests proving organization/workspace/member isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement automatic organization/workspace bootstrap after first auth</name>
  <files>apps/web/src/lib/supabase/admin.ts, apps/web/src/lib/tenant/bootstrapWorkspace.ts, apps/web/src/lib/tenant/bootstrapWorkspace.test.ts, apps/web/src/app/auth/finish-signup/route.ts</files>
  <behavior>
    - Test 1: a first-time authenticated user gets one organization, one workspace, and one owner membership
    - Test 2: rerunning the bootstrap for the same user is idempotent and creates no duplicate workspace
    - Test 3: the finish-signup route redirects existing members to their current workspace instead of provisioning again
  </behavior>
  <action>Create a server-only Supabase admin client using `SUPABASE_SERVICE_ROLE_KEY`, then implement `bootstrapWorkspaceForUser()` to execute the discovery choice for onboarding: automatic org + workspace + owner membership creation per D-08. Use the smallest transactional path possible and keep the workspace slug as the canonical subdomain key per D-06. Add `auth/finish-signup/route.ts` that is called after auth callback when the user has no membership yet. This task must not add invite flows, billing setup, or multi-workspace switching.</action>
  <verify>
    <automated>npm --prefix apps/web run test:db && npm --prefix apps/web test -- src/lib/tenant/bootstrapWorkspace.test.ts</automated>
  </verify>
  <done>First login can deterministically provision exactly one tenant context, and repeats are safe.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/web run test:db`
- `npm --prefix apps/web test -- src/lib/tenant/bootstrapWorkspace.test.ts`
- Confirm all public tables created in the migration have `enable row level security`
</verification>

<success_criteria>
- Tenant data model exists without overcommitting deferred product schema
- RLS blocks cross-workspace access by default
- First-login provisioning matches the single-workspace rule and is idempotent
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-03-SUMMARY.md`
</output>
