---
phase: 05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/package.json, apps/web/tsconfig.json, apps/web/next.config.mjs, apps/web/vitest.config.ts, apps/web/.env.example, apps/web/src/lib/env.ts, apps/web/src/lib/env.test.ts, apps/web/src/app/layout.tsx, apps/web/src/app/globals.css, apps/web/src/app/(marketing)/page.tsx, apps/web/src/app/not-found.tsx]
autonomous: true
requirements: [SAAS-01, SAAS-05]
must_haves:
  truths:
    - "Yeni SaaS uygulaması mevcut SPA'dan ayrı bir proje olarak çalıştırılabilir"
    - "Root domain üzerinde ortak landing sayfası render edilir"
    - "Supabase/Vercel/AI secret contract'i tek yerde doğrulanır"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Next.js app runtime, scripts, and dependency contract"
      contains: "next"
    - path: "apps/web/src/lib/env.ts"
      provides: "typed public/server environment parsing"
      exports: ["getPublicEnv", "getServerEnv"]
    - path: "apps/web/src/app/(marketing)/page.tsx"
      provides: "shared landing page shell"
  key_links:
    - from: "apps/web/package.json"
      to: "apps/web/vitest.config.ts"
      via: "test script"
      pattern: "\"test\""
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/app/globals.css"
      via: "global styling import"
      pattern: "globals\.css"
    - from: "apps/web/src/lib/env.ts"
      to: "apps/web/.env.example"
      via: "documented variable names"
      pattern: "SUPABASE|GEMINI|FAL|ROOT_DOMAIN"
---

<objective>
Create the new `apps/web` application foundation so Phase 5 work happens in a separate SaaS project, with a typed env contract, test harness, and root landing shell.

Purpose: per D-01, D-02, D-09, and D-10 the repo needs a new deployable app boundary before auth, tenant, and backend work can land safely.

Output: standalone Next.js app scaffold, env parser/tests, and marketing landing shell.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-CONTEXT.md
@.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-DISCOVERY.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the new app workspace and env contract</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/web/next.config.mjs, apps/web/vitest.config.ts, apps/web/.env.example, apps/web/src/lib/env.ts, apps/web/src/lib/env.test.ts</files>
  <behavior>
    - Test 1: missing required public vars fail fast with a descriptive error
    - Test 2: missing required server-only vars fail fast with a descriptive error
    - Test 3: `apps/web` exposes `dev`, `build`, `test`, `typecheck`, and `test:db` scripts for downstream plans
  </behavior>
  <action>Create a fresh `apps/web` app per D-01 instead of editing the current root SPA. Use Next.js App Router on React 19 with TypeScript, `@supabase/supabase-js`, `@supabase/ssr`, `zod`, and Vitest. Add package scripts for `dev`, `build`, `test`, `typecheck`, and `test:db` so later plans can verify auth/db work without inventing tooling. Implement `src/lib/env.ts` as the single typed source of truth for `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_ROOT_DOMAIN`, `SUPABASE_SERVICE_ROLE_KEY`, `GEMINI_API_KEY`, and `FAL_KEY`; keep service-role and provider keys server-only per D-10. Add focused env parsing tests in `src/lib/env.test.ts`. Do not add tenant logic, auth flows, or database schema in this task.</action>
  <verify>
    <automated>npm --prefix apps/web test -- src/lib/env.test.ts</automated>
  </verify>
  <done>`apps/web` exists as a standalone app workspace, downstream verification scripts are in place, and env parsing prevents accidental secret drift.</done>
</task>

<task type="auto">
  <name>Task 2: Add the root landing shell and shared app layout</name>
  <files>apps/web/src/app/layout.tsx, apps/web/src/app/globals.css, apps/web/src/app/(marketing)/page.tsx, apps/web/src/app/not-found.tsx</files>
  <action>Create the minimal shared shell that proves D-07 and D-09 are represented in code: a root layout, global styles, a root-domain landing page, and a not-found screen. The landing page should explicitly position the app as the secure SaaS successor to the current fit-check experience, with CTAs reserved for login/signup that later plans will wire. Keep the page intentionally lightweight; do not port the existing styling workflow UI or Tailwind CDN setup. Use plain Next.js/global CSS so the scaffold stays small and buildable.</action>
  <verify>
    <automated>npm --prefix apps/web run build</automated>
  </verify>
  <done>The new app builds cleanly and serves a root landing shell distinct from the existing SPA.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/web test -- src/lib/env.test.ts`
- `npm --prefix apps/web run build`
- Confirm the existing root-level Vite app files are untouched
</verification>

<success_criteria>
- `apps/web` is independently installable and buildable
- Environment variables are validated before auth/backend code is added
- Root domain has a concrete landing shell for future auth entry
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi/05-backend-guvenligi-api-key-izolasyonu-multi-tenant-saas-mimarisi-ve-postgresql-stratejisi-01-SUMMARY.md`
</output>
